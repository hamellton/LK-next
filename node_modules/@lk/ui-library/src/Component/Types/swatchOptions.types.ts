import { PriceType } from "./price.types";

export interface SwatchOptionType{
	id: number
	color: string
	glassColor?: string
	colorID: number
	productURL: string
	price: PriceType
	arModel: ARModalType,
	productImage: ProductImageType
	frameColorImage: string
}

interface ARModalType {
	ios: string | null,
	android: string | null
}

interface ProductImageType{
	url: string
	hoverURL: string
}

