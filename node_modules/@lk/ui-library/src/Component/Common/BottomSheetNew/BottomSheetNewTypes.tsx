import { ReactNode } from "react";

export interface BottomSheetNewTypes {
	show: boolean;
	closeBottomSheet: () => void;
	children: ReactNode;
	backdrop: boolean;
	fullPage: boolean;
	borderRadius: boolean;
}