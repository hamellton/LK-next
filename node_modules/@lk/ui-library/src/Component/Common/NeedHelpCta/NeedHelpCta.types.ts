import { JSON } from "../../../Types/general";

export interface NeedHelpCtaProps {
  localeData: JSON;
  isRendered: boolean;
  isBuyOnChat: boolean;
  whatsappChatMsg: string;
  phoneNumber: string;
  onClick?: () => void;
}