import styled from "styled-components";
import { fontSize } from "./helper";
import { ComponentSizeENUM } from "../../../Types/general";


export const TextareaField= styled.textarea<{error:boolean, disabled:boolean, isRTL: boolean}>`
font-size:1em;
border: none;
background:none;
border-bottom: 1px solid ${props => props.error ? "var(--error)" : "var(--serene-gray)"};
padding: 0.5em 0.6em 0.4em 0;
width: 100%;
direction: ${props => props.isRTL ? "rtl" : ""};
display: block;
outline: none;
color: var(--dark-grey);
cursor: ${(props) => (props.disabled ? "not-allowed" : "auto")};
resize: none;

&::placeholder{
	color:var(--placeholder);
}
user-select: initial;
	&::before, &::after {
    	user-select: initial;
	}
`;

export const TextareaLabel= styled.label<{isRTL: boolean}>`
font-size: 1em;
position: absolute;
top: 0.5em;
left:${props => props.isRTL ? "" : "0"};
right: ${props => props.isRTL ? "0" : ""};
pointer-events: none;
-webkit-transition: 0.2s ease all;
color: var(--dark-grey);
transition: 200ms cubic-bezier(0, 0, 0, 1) 0ms;
`;


export const TextareaContainer = styled.div<{width:string, styleSize:ComponentSizeENUM}>`
font-size: var(${(props) => fontSize[props.styleSize]});
position: relative;
display: flex;
height:max-content;
flex-direction: column;
width: ${(props) => props.width};

:focus-within ${TextareaLabel}{
	transform: translate(0, -1em);
	font-size:0.8em;
	font-weight:var(--fw-regular);
}`;

export const ErrorMessage = styled.p`
	color:var(--error);
	margin:0;
	padding-top:var(--pd-5);
	font-size:0.8em;
`;
