import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes } from "../../../StorybookHelper";
import Timer from "./index";

export default {
	title: "Components/common/Timer",
	component: Timer,
	argTypes: {
		initialTimeInSeconds: generateArgTypes({
			type: "number",
			required: true,
			description: "time in seconds from where the timer starts.",
			category: "Component",
		}),
	}
} as ComponentMeta<typeof Timer>;

const Template: ComponentStory<typeof Timer> = (args) => <Timer {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	initialTimeInSeconds: 300
};
