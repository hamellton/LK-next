import styled from "styled-components";

export const DividerWrapper = styled.div<{styleColor: string | undefined }>`
	border: none;
	height: 1px;
	margin: 0;
	flex-shrink: 0;
	background-color: ${({ styleColor }) => styleColor ? styleColor : "var(--border)"};
`;
