import React from "react";
import { DividerTypes } from "./Divider.types";
import { DividerWrapper } from "./styles";

const Divider = ({ id, color }: DividerTypes) => {
	return <DividerWrapper styleColor= {color} data-testid="divider-test" id={id}></DividerWrapper>;
};

export default Divider;

