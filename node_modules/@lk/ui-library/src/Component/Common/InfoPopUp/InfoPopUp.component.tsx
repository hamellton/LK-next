import React from "react";
import { Root } from "./InfoPopUp.styles";

const InfoPopUp = ({ message, backgroundColor, color }: {message: string, backgroundColor: string, color: string}) => {
	return (
		<Root color={color} backgroundColor={backgroundColor}>
			<div>{message}</div>
		</Root>
	);
};

export default InfoPopUp;
