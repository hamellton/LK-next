import styled, { keyframes } from "styled-components";

export const breatheAnimation = keyframes`
 0% { top: 0px; opacity: 0.1;}
 100% { top: 20px; opacity: 1; }
`;

export const Root = styled.div<{color: string, backgroundColor: string}>`
    position: fixed;
    right: 10px;
    min-width: 300px;
    padding: 15px;
    border-radius: 6px;
    top: 20px;
    opacity: 1;
    z-index: 101201020;
    color: ${props => props.color};
    background-color: ${props => props.backgroundColor};
    animation: ${breatheAnimation} 1s ease;
`;
