import { ReactNode } from "react";

export interface CollapsibleSideBarTypes {
	id: string;
	// width:number;
	children: ReactNode
	xPosition: number
	overLay: boolean
	height: number;
	title: string;
	isRTL?: boolean;
	mobileView?: boolean;
	onClose: () => void;
	hideArrowBackground?: boolean
	backdropClick?: (props: boolean) => void
	width?: string;
	backgroundColor?: string;
	backDrop?: string 
}
