import { ReactNode } from "react";
import { DataType } from "../../../Types/general";

export interface ApplyCouponType {
	mobileView: boolean;
	children: ReactNode;
	appliedCoupon?: string;
	applyGvHandler: (code: string, flag: string) => void;
	applicableGvs: Array<applicableGvObject>;
	sorryNoCoupon: string;
	showSidebar: boolean;
	setShowSidebar: (showSidebar: boolean) => void;
	errMsg: string,
	setErrorMsg: (errMsg: string) => void;
	isRTL?: boolean;
	textCapitalize?: boolean;
	hideArrowBackground?: boolean;
	dataLocale?: DataType;
	newCartOfferDesign?: boolean
	bestOffers?: Array<applicableGvObject>;
	bankOffers?: Array<applicableGvObject>;
}

export interface applicableGvObject {
	code: string;
	priority: number;
	description: string;
	termsAndConditions: string;
	autoApplicable: boolean;
	heading: string;
	brandImage?: string;
	paymentOffer: boolean
  }
