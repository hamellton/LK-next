import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes } from "../../../StorybookHelper";
import Snackbar from "./index";

export default{
	title: "Components/Common/SnackBar",
	component: Snackbar,
	argTypes: {
		id: generateArgTypes({
			type: "string",
			required: false,
			description: "String used as unique identifier",
			category: "General"
		}),
		state: generateArgTypes({
			type: "string",
			required: true,
			description: "show or hide spinner",
			category: "Component",
			defaultValue: true
		}),
		message: generateArgTypes({
			type: "string",
			required: false,
			description: "show spinner full page or not",
			category: "Component",
			defaultValue: false
		})
	}
} as ComponentMeta<typeof Snackbar>;

const Template: ComponentStory<typeof Snackbar> = (args) => <Snackbar {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	state: "Success",
	message: "Your Successfull"
};