import styled from "styled-components";
import { TypographyENUM } from "../../../Types/general";

export const OutOfStockFormContainer = styled.form<{styleFont:TypographyENUM, isRTL:boolean}>`
	display:flex;
	flex-direction:column;
	gap:var(--spacing-md);
	font-size:var(--fs-13);
	font-family:${props => props.styleFont};
	width:100%;
    background: #fcfcfc;
    border: 1px dashed #ccc;
    padding: 10%;
    margin-left: -5px;
    margin-right: -5px;
`;

export const InputWrapper = styled.div`
	position:relative;
	width:100%
    margin
`;

export const HeadingWrapper = styled.div`
    font-size: 18px;
    color: red;
    text-align: center;
`;

export const ParagraphWrapper = styled.div<{error: boolean}>`
    font-size: 12px;
    color: ${props => props.error ? "red" : "black"};
    text-align: center;
`;
