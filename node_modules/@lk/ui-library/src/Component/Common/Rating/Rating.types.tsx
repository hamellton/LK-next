import { ComponentSizeENUM } from "../../../Types/general";

export interface RatingTypes{
	id:string;
	rating:number;
	maxRating:number;
	componentSize: ComponentSizeENUM;
	isReadOnly:boolean;
	onStarsChange: (prop: number) => void;
	isRTL: boolean;
	color?: string;
}
