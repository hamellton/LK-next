export interface ToggleButtonTypes {
    text: string;
    checked: boolean;
    clickOnToggle: () => void;
    outerWidth: number;
    outerHeight: number;
    innerWidth: number
    innerHeight: number
    innerColor: string
    outerColor: string
}