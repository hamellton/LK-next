import React from "react";
import { SecureCard } from "../../../Icons";
import { CardImg, CloseBtn, Heading, ListPoints, OverLay, SubText } from "./SecureCardModalStyles";
import { MoneyModalType } from "./SecureCardModalType";

const MoneyModal = ({ id, isVisible, closeModal, localeData }: MoneyModalType) => {
	return (
		<>
			{isVisible && (
				<>
					<OverLay id={id}>
						<div className="modalWrapper">
							<CardImg>
								<SecureCard/>
							</CardImg>
							{/* <img src="https://preprod.lenskart.com/secure_card/secure_card-e6e0f63aaa661b2160181080c4dcf49e.svg" alt="secure_card_img" /> */}
							<Heading>
								{localeData?.SECURE_YOUR_CARD}
							</Heading>
							<SubText>
								{localeData?.RBI_GUIDELINES}
							</SubText>
							<ul>
								<ListPoints>
									{localeData?.RBI_GUIDELINE_1}
								</ListPoints>
								<ListPoints>
									{localeData?.RBI_GUIDELINE_2}
								</ListPoints>
							</ul>
							<CloseBtn onClick={closeModal}>{localeData?.OKAY_GOT_IT}</CloseBtn>
						</div>
					</OverLay>
				</>
			)}
		</>
	);
};

export default MoneyModal;
