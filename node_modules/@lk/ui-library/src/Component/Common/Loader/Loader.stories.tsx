import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import CommonLoader from "./index";

export default {
	title: "Components/Common/Loader",
	component: CommonLoader,
} as ComponentMeta<typeof CommonLoader>;

const Template: ComponentStory<typeof CommonLoader> = () => <CommonLoader show={false} />;

export const Overview = Template.bind({});
