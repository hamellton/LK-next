import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import { generateArgTypes } from "../../../StorybookHelper";
import TryOn from "./index";

export default {
	title: "Components/common/TryOn",
	component: TryOn,
	argTypes: {
		id: generateArgTypes({
			type: "string",
			required: true,
			description: "String used as unique identifier",
			category: "General",
		}),
		isRTL: generateArgTypes({
			type: "boolean",
			required: true,
			description: "show try on right to left",
			category: "Status",
		}),
	},
} as ComponentMeta<typeof TryOn>;

const Template: ComponentStory<typeof TryOn> = (args) => <TryOn {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	id: "try-on-btn",
	width:"119px",
	tryOnImageURL:"https://static.lenskart.com/media/desktop/img/desktop_pdp_model.png",
	imagePosition:"0",
	height:"34px"
};
