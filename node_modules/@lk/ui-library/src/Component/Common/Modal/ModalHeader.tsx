import React from "react";
import { ModalHeaderProps } from "./Modal.types";

const ModalHeader = (props: ModalHeaderProps) => {
	const {
		children,
		closeButton,
		onHide,
		voucherModal = false,
		isRTL = false,
	} = props;
	return (
		<>
			<div className="modal-header">
				{closeButton && (
					<button className="close" onClick={() => onHide()} style={{ left: isRTL ? "20px" : "", right: isRTL ? "" : "20px" }}>
						<span style={{ fontSize: voucherModal ? "26px" : "" }}>×</span>
					</button>
				)}
				{children || null}
			</div>
		</>
	);
};

export default ModalHeader;
