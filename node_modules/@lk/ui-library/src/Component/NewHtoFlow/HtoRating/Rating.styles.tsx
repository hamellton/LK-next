import styled from "styled-components";
import { TypographyENUM } from "../../../Types/general";

export const RatingWrapper = styled.div`
	display:flex;
	flex-direction: row;
`;

export const RatingText = styled.div`
	font-family: ${TypographyENUM.lkSansMedium};
	font-style: normal;
	font-weight: 500;
	font-size: 12px;
	line-height: 16px;
	margin-left: 5px;
	color: var(--dark-blue-100);
`;

export const RatingContainer = styled.div<{isReadOnly:boolean, isRTL: boolean}>`
display:flex;
align-items:center;
flex-wrap:wrap;
/* gap:0.25em; */
cursor:${props => props.isReadOnly ? "default" : "pointer"};
font-size: var(--fs-15);
${props => props.isRTL ? "flex-direction: initial" : ""};
svg {
padding: 0 0.25em 0 0;
box-sizing: content-box;
}
`;

export const RatingCount = styled.div`
font-style: normal;
font-weight: var(--fw-bold);
font-size: var(--fs-12);
line-height: 16px;
display: flex;
align-items: center;
text-decoration-line: underline;
color: #66668E;
font-family: ${TypographyENUM.lkSansBold};
margin-left: 5px;
`;
