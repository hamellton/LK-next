import { DataType } from "../../../Types/general";

export interface ProductNamePriceType {
    heading: string
    subHeading: string;
    actualPrice: number;
    sellingPrice: number;
    dataLocale: DataType
}