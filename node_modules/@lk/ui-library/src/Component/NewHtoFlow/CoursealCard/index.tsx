import React from "react";
import { CousealCardType } from "./Courseal.types";
import { CoursealCardWrap, IconWrapper, TextWrapper, TextDiv } from "./styles";

const CoursealCard = ({ heading1, heading2, icon }: CousealCardType) => {
	return (
		<CoursealCardWrap>
			<IconWrapper>
				<img src={icon} alt="iconImage" />
			</IconWrapper>
			<TextWrapper>
				<TextDiv>{heading1}</TextDiv>
				<TextDiv>{heading2}</TextDiv>
			</TextWrapper>
		</CoursealCardWrap>
	);
};

export default CoursealCard;