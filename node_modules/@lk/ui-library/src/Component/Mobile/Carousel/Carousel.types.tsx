import { ReactNode } from "react";
import { DataType } from "../../../Types/general";

export interface CarouselPropTypes {
    autoPlay: number, 
    disabledInfinite: boolean, 
    slidesPerView: number, 
    onSlideChange: ({ currentSlide }: {currentSlide: number}) => void, 
    children: ReactNode[], 
    newStyle?: object, 
    dots: boolean, 
    arrows: boolean, 
    dataLocale: DataType, 
    isPfu: boolean, 
    noOfSlides: number, 
    slideStyle?: any, 
    isFitToView: boolean, 
    leftMirroredSlides: any, 
    rightMirroredSlides: any
}