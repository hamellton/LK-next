import { ReactNode } from "react";

export interface BottomSheetTypes {
    show?: boolean
    closebottomSheet?: () => void;
    children: ReactNode
    bottomSheetCss?: any,
    backdrop: boolean,
    onClickBackdrop?: () => void,
    expanded: boolean,
    addTopRadius?: boolean,
    pageContext?: string,
    sixOverSix?: boolean,
    borderRadius: boolean,
    addressEditOrderDetail?: boolean,
    backdropCss?: any,
	darkBack?: boolean
}
