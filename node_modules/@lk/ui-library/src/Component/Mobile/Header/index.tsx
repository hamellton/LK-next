import React from "react";
import * as Icons from "../../../Icons/index";
import { HeaderIcon, HeaderLogo, HeaderMenu, HeaderRightItem, HeaderRightWrapper, HeaderWrapper } from "./styles";
import { topLinks } from "./mockValues";
import { HeaderDataType } from "./Header.types";

const Header = ({ onMenuClick }: HeaderDataType) => {

	return (
		<HeaderWrapper>
			<HeaderMenu>
				<HeaderIcon onClick={() => onMenuClick}>
					<Icons.Home />
				</HeaderIcon>
			</HeaderMenu>
			<HeaderLogo>
				<a href={topLinks.left.logo.link}><img alt={topLinks.left.logo.alt} width={topLinks.left.logo.width} src={topLinks.left.logo.img} />
				</a>
				{topLinks.left.toll_free.showTollFree && <a aria-label={topLinks.left.toll_free.alt} href={topLinks.left.toll_free.link}><img width={topLinks.left.toll_free.width} alt={topLinks.left.toll_free.alt} src={topLinks.left.toll_free.img} /></a>}
			</HeaderLogo>
			<HeaderRightWrapper>
				<HeaderRightItem>
					<img alt={topLinks.right.qr_scanner.alt} src={topLinks.right.qr_scanner.img} width={topLinks.right.qr_scanner.width} height={topLinks.right.qr_scanner.height} />
				</HeaderRightItem>
				<a href={topLinks.right.wishlist.link}>
					<HeaderIcon>
						<Icons.HeartFilled />
					</HeaderIcon>
				</a>
				<HeaderIcon>
					<Icons.WhatsAppIcon />
				</HeaderIcon>
				<a href={topLinks.right.cart.link}>
					<HeaderIcon>
						<Icons.EmptyCart />
					</HeaderIcon>
				</a>
			</HeaderRightWrapper>
		</HeaderWrapper>
	);
};

export default Header;
