import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes, generateGeneralArgs } from "../../../../StorybookHelper";
import Timer from "./";

export default {
	title: "Components/V1/SignInForm/Timer",
	component: Timer,
	argTypes:{
		...generateGeneralArgs(),
		minutes:generateArgTypes({
			type: "number",
			required: true,
			description: "minutes",
			category: "Component",
			defaultValue: "0",
		}),
		seconds:generateArgTypes({
			type: "number",
			required: true,
			description: "seconds",
			category: "Component",
			defaultValue: "0",
		}),
	}
} as ComponentMeta<typeof Timer>;

const Template: ComponentStory<typeof Timer> = (args) => <Timer {...args}/>;
export const Overview = Template.bind({});

Overview.args = {
	id:"timer",
	minutes:0,
	seconds:5
};

