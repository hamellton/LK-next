import React from "react";
import styled from "styled-components";
// import { DataType } from "../../../Types/general";

export const ShowFloatingWhatsAppIcon = styled.a`
  background-color: #a9f1a9;
  border-radius: 15px 0 0 15px;
  border: 2px solid #a9f1a9;
  border-right: 0;
  bottom: 98px;
  right: 0;
  z-index: 99;
  position: fixed;
  img {
    padding: 7px 8px 2px 8px;
  }
`;

export interface FloatingBuyOnChatProps {
  whatsAppChatMsg: string;
  imgSrc: string;
  onClickHandler?: () => void
}

const FloatingBuyOnChat = ({ whatsAppChatMsg, imgSrc, onClickHandler }: FloatingBuyOnChatProps) => {
	return (
		<ShowFloatingWhatsAppIcon
			href={whatsAppChatMsg}
			onClick={onClickHandler}
		>
			<img
				alt="Chat"
				src={imgSrc}
			/>
		</ShowFloatingWhatsAppIcon>
	);
};
export default FloatingBuyOnChat;