import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import MobileEmptyCart from "./MobileEmptyCart.component";

export default {
	title: "Components/v1/Cart/MobileEmptyCart",
	component: MobileEmptyCart,
	args : {

	},
} as ComponentMeta<typeof MobileEmptyCart>;

const Template: ComponentStory<typeof MobileEmptyCart> = (args) => <MobileEmptyCart {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	mobileView: true,
};
