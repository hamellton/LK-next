import React from "react";
import { ComponentSizeENUM, TypographyENUM } from "../../../../Types/general";
import Button from "../../Button";
import { NotFoundTypes } from "./NotFound.types";
import { ButtonBox, Heading } from "./styles";

const NotFound = ({ 
	dataLocale = {
		ERROR_404: "Your shopping cart is empty",
		ERROR_500: "Internal Server Error",
		CONTINUE_SHOPPING: "Continue Shopping",
	},
	// cartStatusCode = 0,
	cartErrorMessage = "",
	link = () => console.log("")

}: NotFoundTypes) => {
	return (
		<div>
			<Heading>{dataLocale[cartErrorMessage] || dataLocale.ERROR_404} !</Heading>
			{/* {(cartStatusCode === 404 || cartStatusCode === 401) && ( */}
			<ButtonBox>
				<Button
					width="100"
					componentSize={ComponentSizeENUM.small}
					font={TypographyENUM.lkSansBold}
					text={dataLocale.CONTINUE_SHOPPING}
					onClick={() => {
						link();
					} }
					style={{ borderRadius: "30px" }}
					fontSize={24}
					id={""}
				/>
			</ButtonBox>
			{/* )} */}
		</div>
	);
};

export default NotFound;