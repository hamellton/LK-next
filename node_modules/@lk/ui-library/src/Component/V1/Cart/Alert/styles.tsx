import styled from "styled-components";
import { ComponentSizeENUM, TypographyENUM } from "../../../../Types/general";
import { fontSize, height } from "./helper";

export const AlertWrapper = styled.div<{bgColor: string, styleSize: ComponentSizeENUM, styleFont: TypographyENUM, boxShadow: boolean}>`
	display: flex;
	justify-content: center;
	align-items: center;
	padding: var(--pd-8) var(--pd-12);
	background: ${(props) => props.bgColor};
	font-weight: var(--fw-bold);
	color: #333368;
	// height: var(${(props) => height[props.styleSize]});
	font-size: var(${(props) => fontSize[props.styleSize]});
	border-radius: var(--border-radius-xxs);
	font-family:  ${(props) => props.styleFont};
	${props => props.boxShadow && "box-shadow: 0 1px 4px rgb(0 0 0 / 10%);"}
`;
