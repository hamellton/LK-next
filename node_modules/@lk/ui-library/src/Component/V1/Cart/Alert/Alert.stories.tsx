import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import {
	generateArgTypes,
	generateGeneralArgs,
} from "../../../../StorybookHelper";
import Alert from "./index";
import { AlertColorsENUM, ComponentSizeENUM, TypographyENUM } from "../../../../Types/general";
export default {
	title: "Components/v1/Cart/Alert",
	component: Alert,
	argTypes: {
		...generateGeneralArgs(),
		color: generateArgTypes({
			type: "string",
			required: true,
			description: "Color",
			category: "General",
			control: "select",
			options: AlertColorsENUM,
		})
	},
} as ComponentMeta<typeof Alert>;

const Template: ComponentStory<typeof Alert> = (args) => <Alert {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	id: "Alert",
	children: "Extra 70% off using code EYECONN",
	color: AlertColorsENUM.blue,
	componentSize: ComponentSizeENUM.small,
	font: TypographyENUM.defaultBook
};

