import React, { useState } from "react";
import {
	CouponWrapper,
	FlexWrapper,
	FlexColWrapper,
	ApplyButton,
	// ReadMoreButton,
	DashedHR,
	CouponHeader,
	ApplyButtonNew,
	FlexWrapperNew,
	FlexWrapperNewText,
	TermsCondition,
	BrandImageWrapper
} from "./styles";
import { CouponTypes } from "./Coupon.types";
import { TypographyENUM } from "../../../../Types/general";

/**
 * Default Coupon Component
 */
const Coupon = ({
	id,
	width,
	// headText,
	subText,
	descText,
	termsAndCondition,
	onClick,
	brandImage,
	headText,
	mobileView,
}: // font
CouponTypes) => {
	// const [showMore, setShowMore] = useState(false);
	const[terms,setTerms] = useState(false);
	function showTerms(){
		setTerms(!terms);
	}

	return (
		<div>
			{!mobileView && <CouponWrapper
				id={id}
				width={width}
				styledFont={TypographyENUM.lkSansRegular}
				mobileView={mobileView}
			>
				<FlexColWrapper>
					<FlexWrapper>
						{/* <FlexWrapper isGap={true} isBold={true}> */}

						<FlexWrapper isBold={true} isSubText={true}>{subText}</FlexWrapper>
						{/* </FlexWrapper> */}
						<ApplyButton onClick={onClick}>Apply</ApplyButton>
					</FlexWrapper>
					<FlexWrapper isBold={true} isDescText={false}>{descText}</FlexWrapper>
				</FlexColWrapper>
				<DashedHR mobileView={mobileView}/>
				<FlexWrapper isDescText={true}>{termsAndCondition}</FlexWrapper>

				{/* <ReadMoreButton onClick={() => setShowMore(!showMore)}>{!showMore ? "+ See more": "See less"}</ReadMoreButton> */}
			</CouponWrapper>}
			{mobileView && <CouponWrapper
				id={id}
				width={width}
				styledFont={TypographyENUM.lkSansRegular}
				mobileView={mobileView}
			>
				<FlexColWrapper>
					<FlexWrapper>
						{/* <FlexWrapper isGap={true} isBold={true}> */}
						{brandImage && <BrandImageWrapper brandImage={brandImage} />}
						<CouponHeader brandImage={brandImage}>{headText}</CouponHeader>

						{/* </FlexWrapper> */}
						{/* <ApplyButton onClick={onClick}>APPLY</ApplyButton> */}
						<ApplyButtonNew onClick={onClick}>Apply</ApplyButtonNew>
					</FlexWrapper>

				</FlexColWrapper>
				<DashedHR mobileView={mobileView}/>
				<FlexWrapperNew>{subText}</FlexWrapperNew>
				<FlexWrapperNewText>{descText}</FlexWrapperNewText>
				{!terms && <TermsCondition onClick={showTerms}>Terms & Conditions</TermsCondition>}
				{terms && <FlexWrapperNewText>{termsAndCondition}</FlexWrapperNewText>}
				{terms && <TermsCondition onClick={showTerms}>See less</TermsCondition>}
			</CouponWrapper>}
		</div>
	);
};
export default Coupon;
