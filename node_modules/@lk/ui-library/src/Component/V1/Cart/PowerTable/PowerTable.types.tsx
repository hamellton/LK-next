import { DataType } from "../../../../Types/general";

export interface PowerTableType {
	heading: {
		Eye: string,
		SPH?: string,
		CYL?: string,
		Axis?: string,
		Boxes: string,
	},
	data: Array<{
		Eye?: string,
		SPH?: string,
		CYL?: string,
		Axis?: string,
		Boxes?: number,
	} > | null,
	fullWidth?: boolean,
	boxMax?: boolean,
	userName?: string,
	showTable?: boolean,
	showFunction?: any;
	dataLocale: DataType;
}
