import { TypographyENUM } from "../../../../Types/general";

export interface PriceBreakupTypes {
	id: string
	width?: string
	dataLocale: any
	font: TypographyENUM,
	priceData: CartTotalType[],
	onShowCartBtnClick: () => void;
	cartAmount?: number;
	showPolicy?: boolean;
	showCart?: boolean;
	policyLinks?: { termsOfUse: string, privacyPolicy: string }
	isRTL?: boolean;
	currencyCode: string;
	infoText?: string | Array<string>;
	isMobileView?: boolean;
	enableTax: boolean;
}

export interface CartTotalType {
	id: string,
	label: string,
	amount: number,
	type: CartAmountType
	addDashedLine: boolean
}

export enum CartAmountType {
	POSITIVE="positive",
	NEGATIVE="negative",
	FREE="free",
	TOTAL = "total",
	TOTALBTAX = "beforeTaxTotal",

}

export interface cartAmountObject {
	currencyCode: string
	discounts: Array<object>
	shipping: number
	subTotal: number
	taxes: Array<object>
	total: number
	totalDiscount: number
	totalTax: number
}
