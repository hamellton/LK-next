import { TypographyENUM } from "../../../../Types/general";
import { DataType } from "../../Header/Header.types";

export interface OffersTypes {
	id: string
	width?: any
	tagText: string
	headText: string
	subText: string
	descText: string
	font: TypographyENUM
	offerDetail: DataType
	finalTotal: Array<any>
	isRTL?: boolean;
	currencyCode?: string; 
	addOfferToCart: ({ productId }: {productId: string}) => void;
	subdirectoryPath: string
}
