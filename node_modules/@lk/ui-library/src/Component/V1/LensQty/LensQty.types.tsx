import { ComponentSizeENUM, DataType, TypographyENUM } from "../../../Types/general";

export interface LensQtyTypes {
	dataLocale: DataType;
	isRTL?: boolean;
	isBoundary?: boolean;
	placeholder?: string
	type: string
	componentSize?: ComponentSizeENUM
	width: string
	id?: string
	font: TypographyENUM
	onChange?: (value: React.ChangeEvent<HTMLInputElement>) => void
	dir?: string
	inputType?: string;
}

export enum typeENUM {
	text = "text",
	number = "number",
	dir = "auto"
}

export const inputs = {
	type: typeENUM,
};
