import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { LensQty } from "./index";
import { generateArgTypes, generateGeneralArgs } from "../../../StorybookHelper";

export default {
	title: "Components/V1/LensQty",
	component: LensQty,
	argTypes: {
		...generateGeneralArgs(),
		isRTL: generateArgTypes({
			type: "boolean",
			required: true,
			description: "Show to right to left",
			category: "Status",
			defaultValue: false,
		}),
	}
} as ComponentMeta<typeof LensQty>;

const Template: ComponentStory<typeof LensQty> = (args) => <LensQty {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	dataLocale: {
		Qty: "Qty",
	}
};
