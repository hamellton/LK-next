import { ReactNode } from "react";
import { TypographyENUM } from "../../../../Types/general";

export interface AccordionTypes {
	children: ReactNode
	title: string
	isMaxHeight: boolean
	expand: boolean
	showBorderTop: boolean
	canCollapse: boolean
	stylePadding: string
	handleExpand: () => void,
	font?: TypographyENUM
	isRTL: boolean
    btnText: string;
	icon: string;
    backgroundColor: string;
    color: string;
    titleFontSize: number,
    iconColor: IconColor,
    contentBgColor: string
    contentColor: string,
    borderBottomStyle: boolean;
    contentPadding: string,
	headerNavIcon?:boolean,
	isHeaderStyle?:boolean
	width?: string;
	deviceSpecificIssue?: boolean
}

interface IconColor {
    [color:string]: string
}