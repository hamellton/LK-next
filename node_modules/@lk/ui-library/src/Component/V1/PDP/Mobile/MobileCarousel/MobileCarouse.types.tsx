import { ReactNode } from "react";

export interface MobileCarouselTypes{
	id:string
	images?:ImageType[]
	onClickHandler: (index: number) => void
	children?: ReactNode[]
}

interface ImageType{
	alt:string
	source:string
	redirectUrl:string
}
