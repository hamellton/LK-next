import { ComponentSizeENUM, DataType, TypographyENUM } from "../../../../../Types/general";

export interface TechnicalDetailsType{
	id: string,
	pid: number;
	aboutText:string,
	detailsData:string,
	productDetailTitle:string,
	productDetailText?:string,
	componentSize: ComponentSizeENUM,
	font: TypographyENUM,
	specifications: Array<{name: string, value: string, additionalInfoUrl?: {url: string} | any}>
	onClickHandler: (url: string) => any,
	isExchangeFlow?: boolean;
	localeData: DataType;
	isRTL?: boolean;
	className:string;
}
