import styled from "styled-components";
import { TypographyENUM } from "../../../../Types/general";

export const StoreWrapper = styled.div<{styleFont:TypographyENUM, isRTL: boolean}>`
	display:flex;
	align-items: flex-start;
	font-family:${props => props.styleFont};
	flex-direction: ${props => props.isRTL ? "row-reverse": "row"}
`;

export const StoreImage = styled.img`
	border-radius: var(--border-radius-xxxs);
`;

export const StoreInfo = styled.div<{isRTL: boolean}>`
	padding: 0px var(--pd-20);
	display: flex;
	flex-flow: column;
	justify-content: center;
	height: 80px;
	text-align: ${props => props.isRTL ? "end" : ""}
`;

export const StoreInfoText = styled.span`
	font-weight: var(--fw-medium);
	color: var(--gray);
	font-size: var(--fs-13);
	dir: "rtl"
`;

export const StoreInfoLink = styled.a`
	color: var(--irish-blue);
	cursor: var(--pointer);
	font-size: var(--fs-13);
	padding-top: var(--pd-5);
`;
