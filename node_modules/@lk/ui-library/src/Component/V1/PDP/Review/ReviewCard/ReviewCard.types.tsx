import { TypographyENUM } from "../../../../../Types/general";

export interface ReviewCardType {
	reviewId?: string,
    reviewTitle: string,
    reviewDetail: string,
    reviewee: string,
    reviewDate: string,
    noOfStars: number,
    images: string[]
	font:TypographyENUM;
    isRTL:boolean;
}

export enum ReviewerUserType {
    AnonymousUser = "anonymous_user",
    VerifiedReviewer = "verified_reviewer",
}
