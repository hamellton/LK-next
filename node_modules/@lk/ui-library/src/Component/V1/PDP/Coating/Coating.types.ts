export interface Multiprice {
	name: string,
	currency_code: string,
	price: number
}[];
export interface SinglePrice {
	basePrice: number,
	currency: string,
	lkPrice: number,
	symbol: string
}
export interface AddOnType {
	group: string
	hoverImageUrl: string
	id: string
	imageUrl: string
	price: Multiprice | SinglePrice
	subtitle: string
	title: string
}
export interface CoatingTypes {
	addOnData: AddOnType[]
	coatingImg: string,
	gobackText: string,
	onBackPress: () => void,
	coatingTitle: string,
	coatingPrice: string,
	skipCoatingAction: () => void,
	onSelectCoating: (id: string) => void,
	selectedAddOns: string[],
	dataLocale: {[name: string]: string}
}
