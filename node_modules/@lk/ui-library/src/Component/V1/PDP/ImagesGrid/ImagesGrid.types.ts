export interface ImagesGridProps {
  imagesList: Image[];
  itemClassName?: string;
  containerClassName?: string;
  containerId?: string;
  onClick: (id: number) => void;
  altText?:string
}

export type Image = {
  id?: number;
  imageUrl: string;
}