import styled from "styled-components";
import { TypographyENUM } from "../../../../Types/general";

export const ProductDetailsContainer = styled.div<{
	styleFont: TypographyENUM
}>`
	background-color:var(--white);
	display:flex;
	flex-direction:column;
	gap: var(--pd-10);
	padding:var(--pd-15) 0;
	letter-spacing: 1px;
	font-family: ${(props) => props.styleFont};
`;


export const ProductDetailsHeader = styled.div<{isRTL: boolean}>`
	display:flex;
	gap:var(--spacing-md);
	flex-direction: ${props => props.isRTL ? "row-reverse" : ""};
	text-align: ${props => props.isRTL ? "end": ""}
`;


export const IconWrapper = styled.span<{styleSelected: boolean}>`
	height: fit-content;
	cursor: var(--pointer);
	font-size: ${(props) => props.styleSelected ? "var(--fs-26)" : "var(--fs-14)"};
	.wishlist-icon{
		stroke:${(props) => props.styleSelected ? "var(--wishlist)" : "inherited"};
		&:hover {
			stroke: var(--wishlist);
		}
	}
`;
