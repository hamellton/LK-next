import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes } from "../../../../StorybookHelper";
import { ImageModal } from "./ImageModal.component";
import { productListData } from "./stories-data";

export default {
	title: "Components/V1/PDP/ImageModal",
	component: ImageModal,
	argTypes: {
		productList: generateArgTypes({
			type: "array",
			required: true,
			description: "List of products",
			category: "Component",
			defaultValue: [],
		}),
	}
} as ComponentMeta<typeof ImageModal>;

const Template: ComponentStory<typeof ImageModal> = (args) => (<ImageModal {...args} />);

export const Overview = Template.bind({});

Overview.args = {
	productList: productListData()
};
