import { TypographyENUM } from "../../../../Types/general";

export interface ImageModalProps {
  productList: Product[];
  font:TypographyENUM;
  titleFont:TypographyENUM;
  isSelectedImageID: number;
  onClick: (id: number) => void;
  isRTL?: boolean
}

export interface Product {
  id: number;
  imageUrl: string;
  mediumImageUrl: string;
}