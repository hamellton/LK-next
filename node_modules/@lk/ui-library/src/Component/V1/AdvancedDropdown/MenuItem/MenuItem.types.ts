import { ReactNode } from "react";
import { DataType } from "../../Header/Header.types";

export interface MenuItemType {
	value: string | number,
	onChange: (e: any) => void,
	selectedValue: string | number,
	displayVal: string | number,
	disabled?: boolean,
	menuItemStyles: DataType,
	children?: ReactNode,
}
