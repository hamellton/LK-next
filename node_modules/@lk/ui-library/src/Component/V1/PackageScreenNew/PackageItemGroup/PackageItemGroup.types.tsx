import { ComponentSizeENUM, DataType } from "../../../../Types/general";

export interface PackageItemGroupTypes {
	id: string
	componentSize?: ComponentSizeENUM
	onChange: (id: string, title: string) => void
	prescriptionTypeData: Array<prescriptionTypeDataType>,
	selectedId: string | number,
	radioWidth: string | number,
	isRTL: boolean,
	descriptiveText?: string,
	helplineNumber?: string,
	localeData?: DataType
}
export interface prescriptionTypeDataType {
	id: string
	title: string,
	isPackageAvailable: boolean,
	imageUrl: string,
	subText: string
}
