import styled from "styled-components";
import { TypographyENUM } from "../../../../Types/general";

export const PackageItemGroupWrapper = styled.div<{isRTL?: boolean}>`
	display: flex;
	flex-direction: column;
	gap: var(--pd-16);
	padding: 28px 46px var(--pd-20);
	flex: 1;
	direction: ${props => props.isRTL ? "rtl" : "ltr"};
`;
export const DescriptiveText = styled.div`
	display: flex;
	align-items: center;
	justify-content: center;
	gap: 5px;
`;
export const Text = styled.span<{color?: string, fontWeight?: string}>`
	color: ${props => props.color || "var(--dark-blue-100)"};
	font-family: ${TypographyENUM.lkSansRegular};
	font-weight: 700;
	font-weight: ${props => props.fontWeight || "var(--fw-regular)"};
	font-size: 14px;
	line-height: 20px;
`;
