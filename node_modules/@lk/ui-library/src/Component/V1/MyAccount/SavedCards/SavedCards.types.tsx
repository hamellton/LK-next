export interface SvaedCardTypes {
    dataLocale: any;
    savedCardList: any;
    onDeleteCard: (params:string) => void;
}

export interface CardType {
		cardBrand: string;
		cardMode: string;
		cardToken: string;
		expired: boolean;
		expiryMonth: string;
		expiryYear: string;
		cardNumber: string;
        onDeleteCard: (params:string) => void;
        dataLocale: any;
        key: string
		payLaterAllowed: boolean;
}