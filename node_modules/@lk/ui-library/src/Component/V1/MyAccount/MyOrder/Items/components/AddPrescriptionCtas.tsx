import React from "react";
import { TypographyENUM } from "../../../../../../Types/general";
import styled from "styled-components";

const PdContainer = styled.div`
background-color: #eee;
font-size: 11px;
font-family: ${TypographyENUM.lkSansRegular};
/* width: 95%; */
display: flex;
justify-content: space-between;
align-items: center;
padding: 12px;
margin: 0 10px 10px;
border-radius: 4px;
span {
    cursor: pointer;
    line-height: 16px;
    /* font-weight: 700; */
    text-transform: uppercase;
    color: #00bac6;
    font-size: 12px;
    font-family: ${TypographyENUM.lkSansBold};
}
`;
const AddPrescriptionCtas = ({ /* listing, */onClick, text, btnText,/* orderSuccess */ }: {listing?: boolean, onClick?: () => void, text: string, btnText: string}) => {
	// const handleAddPd = () => {
	// 	let pageName;
	// 	if (orderSuccess) pageName = "post-purchase-order-success";
	// 	else if (listing) pageName = "post-purchase-order-listing";
	// 	else pageName = "post-purchase-order-detail";
	// 	const dlUpdate = {
	// 		event: "cta_click",
	// 		cta_flow_and_page: pageName,
	// 		cta_name: "add-pd",
	// 		customer_id: userInfo?.id,
	// 		lk_user_type: userInfo?.hasPlacedOrder ? "lkRepeat" : "lkNew",
	// 	};
	// 	if (typeof window.dataLayer !== "undefined") window.dataLayer.push(dlUpdate);
	// 	else {
	// 		setTimeout(() => {
	// 			window.dataLayer.push(dlUpdate);
	// 		}, 2000);
	// 	}
	// 	setSheet(true);
	// 	if (preCheckout) {
	// 		setPrescriptionModal(item);
	// 	}
	// };
	return (
		<PdContainer>
			{text}
			<span
				onClick={onClick}
			>
				{btnText}
			</span>
		</PdContainer>
	);
};

export default AddPrescriptionCtas;
