import React from "react";
import { PaymentPendingContainer, PendingIcon, PendingStatusHeading, PendingStatusSubText, PendingStausWrapper, StausSpan } from "././Items/styles";
import { pendingStatusType } from "./MyOrder.types";
import { Image } from "./styles";

const PendingStatus = ({ pendingPowerPaymentStatus, mobileView } : pendingStatusType) => {
	return <PaymentPendingContainer mobileView={mobileView}>
		<PendingIcon mobileView={mobileView}>
			<Image alt="pending status icon" src="https://static.lenskart.com/media/mobile/universal/assets/status-pending.png"></Image>
		</PendingIcon>
		<PendingStausWrapper>
			<StausSpan>
				<PendingStatusHeading mobileView={mobileView}>{pendingPowerPaymentStatus.title}</PendingStatusHeading>
				<PendingStatusSubText mobileView={mobileView}>{pendingPowerPaymentStatus.subTitle}</PendingStatusSubText>
			</StausSpan>
		</PendingStausWrapper>
	</PaymentPendingContainer>;
};

export default PendingStatus;

