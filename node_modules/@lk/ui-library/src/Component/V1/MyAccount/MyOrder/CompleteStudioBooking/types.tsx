import { DataType } from "../../../../../Types/general";

export interface CompleteStudioBookingTypes {
	dataLocale: DataType;
	handleStudioFlowAppointmentClick: (booked: boolean, storeCode: string, id: string | number) => void;
	mobileView?: boolean;
	orderData: any
}