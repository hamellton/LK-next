import React from "react";
import styled from "styled-components";
import { TypographyENUM } from "../../../../../../Types/general";

const ReorderContainer = styled.div`
    border-top: 1px solid #eee;
    user-select: none;
    -webkit-tap-highlight-color: transparent;
    color: #27394e;
    font-size: 11px;
    font-family: ${TypographyENUM.lkSansBold};
	-webkit-font-smoothing: antialiased;
    // font: 12px/1.2857 "Roboto",sans-serif,"Arial","Helvetica",sans-serif;

	.buy-btn-container {
		a{
			font-size:10px;
			font-family: var(--font-lksans-bold);
		}
	}
`;
const ReorderView = styled.div`
    justify-content: space-between;
    padding: 12px;
    align-items: center;
    display: flex;
`;
const PowerAndFaceSection = styled.div`
    background-color: #fff;
    padding-left: 15px;
    padding-right: 15px;
    padding-bottom: 10px;
    border: 0;
// font: 12px/1.2857 "Roboto",sans-serif,"Arial","Helvetica",sans-serif;
    font-size: 12px;
    font-family: ${TypographyENUM.lkSansRegular};
    color: #27394e;
`;
const HeadingsShadow = styled.div`
    padding-top: 10px;
    border-top: 1px solid #eee;
    display: flex;
    justify-content: space-between;
`;
const Reorder = ({ dataLocale, item }: any) => {
	return (
		<ReorderContainer
			data-testid="reorderWrapper"
		>
			<div style={{ width: "100%" }}>
				<ReorderView>
					<div className="msg-container">
						<div style={{ fontWeight: 700 }}>{dataLocale.REORDER_IN_2_MIN}</div>
						<span>
							<a
								aria-label="Click here to call customer support"
								href={`tel:${dataLocale.CUSTOMER_SUPPORT_NUMBER}`}
								style={{ fontWeight: 700, whiteSpace: "nowrap",color: "#00bac6",outline: "none", textDecoration: "none" }}
							>
								{dataLocale.CUSTOMER_SUPPORT_NUMBER}
							</a>
						</span>
					</div>
					<div className="buy-btn-container">
						<a
							aria-label="Click here to reorder same product"
							href={item.productUrl?.replace("https://www.lenskart.com", "")?.replace(window?.location?.href, "")}
							rel="noreferrer"
							target="_self"
							style={{ fontWeight: 700, whiteSpace: "nowrap", color: "#fff", textDecoration: "none", outline: "none", border: "1px solid #00bac6", backgroundColor: "#00bac6", padding: "8px 14px", borderRadius: "4px" }}
							// onClick={this.fireGA.bind(this, item, userInfo, view)}
						>
							{dataLocale.REORDER}
						</a>
					</div>
				</ReorderView>
			</div>
			<PowerAndFaceSection>
				<HeadingsShadow>
					{item.frameWidth && (
						<span
							data-testid="frameWidth"
						>
							<i
							></i>
							{`Size - ${item.frameWidth}`}
						</span>
					)}
					{item.powerRequired === "POWER_SUBMITTED" &&
              item.prescription &&
              ((item.prescription.left && Object.keys(item.prescription.left).length > 0) ||
                (item.prescription.right && Object.keys(item.prescription.right).length > 0)) && (
						<span
							data-testid="powerSubmitted"
						>
							<i></i>
							{dataLocale.POWER_SUBMITTED}
						</span>
					)}
				</HeadingsShadow>
			</PowerAndFaceSection>
		</ReorderContainer>
	);
};

export default Reorder;
