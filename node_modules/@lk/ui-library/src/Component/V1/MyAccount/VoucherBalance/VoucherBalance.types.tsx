export interface VoucherBalanceType {
    dataLocale: any;
    onCheckBalance: (params: any) => void;
    isRTL: boolean,
    errorMessage?: string
}

export enum positionENUM{
	right="right",
	left="left",
}