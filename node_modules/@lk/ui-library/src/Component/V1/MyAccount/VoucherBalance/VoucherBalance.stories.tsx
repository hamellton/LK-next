import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes, generateGeneralArgs } from "../../../../StorybookHelper";
import CheckVoucher from "./index";

export default {
	title: "Components/V1/MyAccount/VoucherBalance",
	component: CheckVoucher,
	argTypes: {
		...generateGeneralArgs(),
		isRTL: generateArgTypes({
			type: "boolean",
			required: true,
			description: "Show right to left onKeepMEPosted",
			category: "Staus",
		}),
	}
} as ComponentMeta<typeof CheckVoucher>;

const Template: ComponentStory<typeof CheckVoucher> = (args) => <CheckVoucher {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	dataLocale: {
		CHECK_VOUCHER_BALANCE: "Check Voucher Balance",
		ENTER_YOUR_VOUCHER_CODE: "Enter your voucher code*",
		ERRORMESSAGE: "This is required",
	},
	onCheckBalance: () => console.log("Voucher is valid"),
};
