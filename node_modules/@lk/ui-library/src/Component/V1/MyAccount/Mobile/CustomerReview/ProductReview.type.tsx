import { ComponentSizeENUM, TypographyENUM } from "../../../../../Types/general";
import { RatingTypes } from "../../../../Common/Rating/Rating.types";

export interface ProductDetailsType extends RatingTypes{
	id: string,
	componentSize: ComponentSizeENUM,
	font?: TypographyENUM,
	// reviewTitle:string,
	dataLocale:ConfigDataType;
	SHARE_YOUR_FEEDBACK:string,
	productImage: string,
	productTitle: string,
	productDec: string,
	PrimaryText:string,
	email: string,
	fullName: string,
	submitFunction: (reviewData: object, emailValue: string) => void;
}

export interface ConfigDataType {
	[name: string]: string
}
