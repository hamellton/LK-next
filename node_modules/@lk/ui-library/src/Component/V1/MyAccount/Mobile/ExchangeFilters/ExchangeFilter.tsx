import React from "react";
import { AccordionTypes } from "./ExchangeFilter.types";
import {
	AccordionWrapper,
	TitleBox,
	TitleWrapper,
	Title,
	Content,
	DownArrow,
	UpArrow,
} from "./styles";

const Accordion = ({
	id,
	children,
	title,
	isMaxHeight,
	expand,
	handleExpand,
	showBorderTop,
	canCollapse,
	isComponent,
	font,
	isRTL = false,
	padding = false,
	height
}: AccordionTypes) => {
	return (
		<AccordionWrapper expand={expand} id={id} borderTopStyle={showBorderTop} styleFont={font} isRTL={isRTL} className="filterWrapper">
			<TitleBox styleComponent={isComponent} onClick={() => handleExpand()} expand={expand} isRTL={isRTL} padding={padding}>
				{canCollapse && <i>{!expand ? <UpArrow /> : <DownArrow />}</i>}
				<TitleWrapper>
					<Title isRTL={isRTL}>{title}</Title>
				</TitleWrapper>
			</TitleBox>
			<Content isMaxHeight={isMaxHeight} height={height} expand={expand} padding={padding} className="accordion-content">{children}</Content>
		</AccordionWrapper>
	);
};

export default Accordion;
