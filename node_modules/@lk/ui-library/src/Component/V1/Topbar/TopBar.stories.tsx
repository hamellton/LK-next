import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes, generateGeneralArgs } from "../../../StorybookHelper";
import TopBar from "./index";

export default {
	title:"Components/V1/TopBar",
	component: TopBar,
	argTypes: {
		...generateGeneralArgs(),
		isRTL: generateArgTypes({
			type: "boolean",
			required: true,
			description: "Show to right to left",
			category: "Status",
			defaultValue: false,
		}),
	}
} as ComponentMeta<typeof TopBar>;

const Template: ComponentStory<typeof TopBar> = (args) => <TopBar {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	dataLocale: {
		FREE_SHIPPING: "FREE SHIPPING | 6 MONTHS WARRANTY| 14 DAYS FREE EXCHANGE",
		TOLL_FREE: "TOLL FREE NUMBER - 9722-2210 (9:30 AM - 9:30 PM)",
	}
};
