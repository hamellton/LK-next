import React from "react";
// import { TypographyENUM } from "../../../Types/general";
import { FreeShippingText, TollFreeText, TopBarWrapper } from "./styles";
import { TopBarTypes } from "./TopBar.types";

const TopBar = (props: TopBarTypes) => {
	const { dataLocale } = props;
	return (
		<TopBarWrapper>
			<FreeShippingText>
				{dataLocale.FREE_SHIPPING}
			</FreeShippingText>
			<TollFreeText>
				{dataLocale.TOLL_FREE}
			</TollFreeText>
		</TopBarWrapper>
	);
};

export default TopBar;
