import React from "react";
import { DataType } from "../../Header.types";
// import { topLinks } from "../../mockValues3";
import { PaddingContainer } from "../../styles";
import { LinedLinks, LinksContainer } from "./styles";

const TopLinks = ({ isRTL = false, topLinks }: {isRTL: boolean, topLinks?: {left: DataType[], right: DataType[]} | undefined}) => {

	return (
		<>{topLinks && <PaddingContainer>
			<LinksContainer>
				<div>
					{topLinks && topLinks?.left?.map((lk, i) => <LinedLinks className="getGaData" isRTL={isRTL} href={lk.url} key={lk.label} line={i!==0}>{lk.label}</LinedLinks>)}
				</div>
				{/* <div>
					{!isRTL && topLinks && topLinks?.right?.map(lk => <LinedLinks href={lk.url} key={lk.label} line={false} isRTL={false}>{lk.label}</LinedLinks>)}
				</div> */}
			</LinksContainer>
		</PaddingContainer>}</>
	);
};

export default TopLinks;
