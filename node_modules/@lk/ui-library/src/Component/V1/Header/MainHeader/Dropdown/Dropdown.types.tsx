export interface SubHeaderDataType {
	data: {
		defaultGender: string | number,
		genders: { id: string | number, text: string | number, url: string, img: string, submenuIds: (string | number)[], defaultCategoryId: string | number }[],
		categories: { id: string | number, text: string | number, subtext: string, submenuIds: (string | number)[] }[],
		subCategories: { id: string | number, text: string | number, submenuIds: (string | number)[] }[]
		links: {
			[key: string | number]: { id: string | number, text: string | number, url: string }
		}
	},
	id?: string | number
	isRTL?: boolean
	setItem?: (lk:any) => void
}
