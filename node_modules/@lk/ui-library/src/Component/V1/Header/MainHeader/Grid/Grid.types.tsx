import { ReactNode } from "react";

export interface GridType {
	col?: number,
	rows?: number,
	gridTemplateArea?: string,
	fixedColumns?: number,
	colWidths?: string,
	defaultColWidth?: string,
	children: ReactNode
}
