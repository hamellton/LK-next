import styled from "styled-components";
import { TypographyENUM } from "../../../../../Types/general";

export const LinksContainer = styled.div`
	display: flex;
	justify-content: var(--space-between);
	width: 100%;
	padding: 8px 10px;
	align-items: center;
	margin-left: 1px
`;

export const LinedLinks = styled.a<{line: boolean; isRTL:boolean}>`
	padding: ${props => props.isRTL ? `0 ${props.line ? "0.5vw" : 0} 0 0.5vw` : `0 0.5vw 0 ${props.line ? "0.5vw" : 0}`};
	${props => props.line && !props.isRTL && "border-left: 1px solid var(--dark-blue-100)"};
	${props => props.line && props.isRTL && "border-right: 1px solid var(--dark-blue-100)"};
	color: var(--dark-blue-100);
	font-size: 10px;
    font-family: ${TypographyENUM.lkSansRegular};
	@media screen and (max-width:1279px){
		padding: ${props => props.isRTL ? `0 ${props.line ? "8px" : 0} 0 8px` : `0 8px 0 ${props.line ? "8px" : 0}`};
	}
`;
