import React from "react";
import { PlaceOrderTextWrapper } from "./placeOrder.styles";

const PlaceOrderText = (props:any) => {
	const {
		// dataLocale,
		paymentCTA, loadingPlaceOrder } = props;
	const paymentText = paymentCTA;
	return (
		<PlaceOrderTextWrapper>
			{loadingPlaceOrder && (
				<div className="loaderLK">
					<div className="barLK"></div>
					<div className="barLK"></div>
					<div className="barLK"></div>
				</div>
			)}
			{!loadingPlaceOrder && (
				<div className={paymentText ? "payment-cta" : ""}>
					<span>
						{/* {paymentText && paymentText.mainText ? paymentText.mainText : dataLocale.PLACE_ORDER} */}
						{paymentText && paymentText.mainText ? paymentText.mainText : "PLACE ORDER"}
					</span>
				</div>
			)}
		</PlaceOrderTextWrapper>
	);
};

export { PlaceOrderText };
