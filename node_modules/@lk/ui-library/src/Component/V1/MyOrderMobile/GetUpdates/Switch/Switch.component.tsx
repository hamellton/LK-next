import React from "react";
import { Container } from "./Switch.styles";
import { SwitchTypes } from "./Switch.types";

const Switch = ({ switchEnabled, onChange }: SwitchTypes) => {
	// function clickHandler () {
	// 	setSwitchEnabled(!switchEnabled);
	// }

	return (
		<Container onClick={onChange} switchEnabled={switchEnabled}>
			<label />
		</Container>
	);
};

export default Switch;
