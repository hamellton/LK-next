import React from "react";
import { Container, Block, Logo, SwitchWrapper } from "./GetUpdates.styles";
import { GetUpdatesTypes } from "./GetUpdates.types";
import Switch from "./Switch/Switch.component";
// active={whatsAppOptingStatus} onChange={chnageWhatsappOptingStatus} disabled={isWhatsappOptingLoading}
const GetUpdates = ({ active, onChange, disabled, dataLocale }: GetUpdatesTypes) => {
	return (
		<Container>
			<Block>
				<Logo>
					<img src="https://static.lenskart.com/media/desktop/img/25-July-19/whatsapp.png" alt="WhatsApp"/>
				</Logo>
				<span>
					{dataLocale?.GET_ORDER_UPDATES_ON_WHATSAPP || "Get Order Updates on WhatsApp"}
				</span>
				<SwitchWrapper>
					<Switch switchEnabled={active} onChange={disabled ? () => null : onChange}/>
				</SwitchWrapper>
			</Block>
		</Container>
	);
};

export default GetUpdates;
