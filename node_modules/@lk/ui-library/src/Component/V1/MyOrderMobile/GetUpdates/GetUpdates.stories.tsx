import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import GetUpdates from "./GetUpdates.component";
import { action } from "@storybook/addon-actions";

export default {
	title: "Components/v1/MyOrder/GetUpdates",
	component: GetUpdates,
	args: {

	},
} as ComponentMeta<typeof GetUpdates>;

const Template: ComponentStory<typeof GetUpdates> = (args) => <GetUpdates {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	active: true,
	onChange: () => action("onChange")("fired"),
	disabled: false,
};
