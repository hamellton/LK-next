import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import TotalPriceCard from "./TotalPriceCard.component";
import { action } from "@storybook/addon-actions";

export default {
	title: "Components/v1/MyOrder/TotalPriceCard",
	component: TotalPriceCard,
	args: {

	},
} as ComponentMeta<typeof TotalPriceCard>;

const Template: ComponentStory<typeof TotalPriceCard> = (args) => <TotalPriceCard {...args} />;

export const Overview = Template.bind({});
Overview.args = {
	totalPrice: "2240",
	currency: "₹",
	viewPriceBreakupHandler: () => action("viewPriceBreakupHandler")("Fired"),
};
