import React from "react";
import SliderCarousel from "../../SliderCarousel";
import { LeftSectionTypes } from "./LeftSection.types";
import { LeftContainer, LeftFlex } from "./styles";

const LeftSection = ({ sliderImages, id, isRTL=false }: LeftSectionTypes) => {
	return (
		<LeftContainer isRTL={isRTL}>
			<LeftFlex>
				<SliderCarousel id={id} sliderImages={sliderImages} duration={2} autoPlay={true} isRTL={isRTL} />
			</LeftFlex>
		</LeftContainer>
	);
};

export default LeftSection;
