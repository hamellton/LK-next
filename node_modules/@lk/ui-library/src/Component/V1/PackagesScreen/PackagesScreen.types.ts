import { ReactNode } from "react";
import { DataType } from "../../../Types/general";
import { SliderImageType } from "../SliderCarousel/SliderCarousel.types";
import { SequenceType } from "./RightSection/RightSection.types";

export interface PackageScreenType {
	id: string,
	rightSectionId: string,
	sequences: SequenceType[],
	heading: string,
	onClosePackages: (props?: number) => void,
	leftSectionId: string,
	sliderImages: SliderImageType[],
	isVisible: boolean,
	children: ReactNode,
	isRTL: boolean
	backButton: (props: number) => void
	selectedPackage?: string
	currentActiveStepType?: string
	localeData: DataType
}
