import { ReactNode } from "react";

export interface RightSectionType {
	id: string,
	heading: string,
	sequences: SequenceType[],
	children: ReactNode,
	onClosePackages: (prop: any) => void,
	backButton: (prop: number) => void
}

export interface SequenceType {
	heading: string,
	selectedText: string,
	showSection: boolean,
	isActive: boolean,
	type:PackageENUM
}

export enum PackageENUM {
	POWER="power",
	PACKAGES="packages",
	COATING="coating",
	EYESIGHT="eyeSight",
}
