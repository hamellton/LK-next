import styled from "styled-components";

export const PackagesContainer = styled.div<{isVisible: boolean, isRTL: boolean;}>`
	display: flex;
	height: 100vh;
	z-index: 3001;
	width: 100vw;
	background-color: #fff;
	position: fixed;
    right: 0;
    background-color: #fff;
    overflow-x: hidden;
    overflow-y: hidden;
    -webkit-transition: fadeIn 1.2s ease-in-out;
    transition: top 350ms ease-in-out, fadeIn 1.2s ease-in-out;
	margin: 0;
	padding: 0;
	top: ${props => props.isVisible ? "0vh" : "100vh"};
	flex-direction: ${ props => props.isRTL ? "row-reverse": "" }
`;
