import React from "react";
import BottomSheet from "../../../Common/BottomSheet/BottomSheet.component";
import { BottomSheetWrapper, Header, Title, Body, Desc, SubmitButton, LaterButton, ButtonContainer } from "./PayLaterBottomsheet.styles";

interface PayLaterBottomsheetTypes {
  closeBottomSheet: ()=>void;
  showBottomSheet: boolean;
  handlePayNow: ()=>void;
  handlePayLater: ()=>void;
}

function PayLaterBottomsheet({ closeBottomSheet, showBottomSheet, handlePayNow, handlePayLater }: PayLaterBottomsheetTypes) {
	return (
		<BottomSheet onBackdropClick={closeBottomSheet} closebottomSheet={closeBottomSheet} show={showBottomSheet} >
			<BottomSheetWrapper>
				<Header>
					<img alt="img" src="https://static.lenskart.com/media/mobile/images/card.svg"></img>
					<Title>
                        Not Ready to Pay Yet?
					</Title>
				</Header>
				<Body>
					<Desc>
                        Visit the showroom first and complete payment after your consultation
					</Desc>
					<Desc>
                        Pay Now to enjoy a more fuss-free experience.
					</Desc>
				</Body>
				<ButtonContainer>
					<SubmitButton onClick={handlePayNow}>
                        Pay Now
					</SubmitButton>
					<LaterButton onClick={handlePayLater}>
                        Book an Appointment & Pay Later
					</LaterButton>
				</ButtonContainer>
			</BottomSheetWrapper>
		</BottomSheet>
	);
}

export { PayLaterBottomsheet };