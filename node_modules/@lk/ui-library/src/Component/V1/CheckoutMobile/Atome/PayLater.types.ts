import { JSON } from "../../../../Types/general";

export interface AtomePropsType {
  localeData: JSON;
  groupData: any;
  isSelected: string;
  onClick: (key: string) => void;
  renderPrimerUI: (methodToShow: string) => void;
  isContactLensConsentEnabled: boolean,
  getSubmitFunction?: (id: any) => void,
  setPayLaterSelected: (val: boolean) => void,
  payLaterBottomsheetHandler: () => void,
  orderCreatedSuccess: boolean,
  setRedirect: (val: boolean) => void
  currencyCode: string;
	totalAmount: string;
  setSectedPrimerCreditCard: (val:boolean) => void
}