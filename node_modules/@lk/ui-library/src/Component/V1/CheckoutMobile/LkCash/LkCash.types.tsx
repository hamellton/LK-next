import { applicableGvObject } from "../../../Common/ApplyCouponBar/ApplyCouponBar.types";

export interface LkCashTypes {
  cartData: any;
  applyGv: any;
  localeData: any;
  isRTL: boolean;
  showSignin?: () => void;
  isGuestUser?: boolean;
  newCartOfferDesign?: boolean
	bestOffers?: Array<applicableGvObject>;
	bankOffers?: Array<applicableGvObject>;
  mergedWithLKCash?: boolean
}