import styled from "styled-components";

export const FloatingSheet = styled.div`
  padding: var(--pd-16) var(--pd-16) var(--pd-32);
  background: var(--white);
  box-shadow: 0px -4px 8px var(--ultra-light-shadow);
  border-radius: var(--border-radius-xs) var(--border-radius-xs) 0px 0px;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index:9;
`;
