import { ComponentMeta, ComponentStory } from "@storybook/react";
import React from "react";
import { generateArgTypes } from "../../../../../StorybookHelper";
import { TypographyENUM } from "../../../../../Types/general";
import { PaymentAnimationLottie } from "./PaymentAnimationLottie";

export default {
	title: "Components/v1/CheckoutMobile/PaymentAnimationLottie",
	component: PaymentAnimationLottie,
	argTypes: {
		font: generateArgTypes({
			type: "string",
			required: true,
			description: "Typography of the component",
			category: "General",
			defaultValue: TypographyENUM.lkSansRegular,
			control: "radio",
			options: TypographyENUM,
		}),

	}
} as ComponentMeta<typeof PaymentAnimationLottie>;

const Template: ComponentStory<typeof PaymentAnimationLottie> = (args) => <PaymentAnimationLottie {...args} />;

export const Overview = Template.bind({});

Overview.args = {};
