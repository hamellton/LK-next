import { JSON } from "../../../../Types/general";
import { StatusType } from "../UPI/UPI.types";

export interface ConfigDataType {
	[name: string]: string
}

export interface AwaitingPaymentTypes{
  dataLocale: ConfigDataType;
	price: string | number;
	currencySymbol: CurrencySymbol;
	getUpiTransactionStatus: any;
	orderId: string;
	configData: JSON;
	upiTransactionStatus: string;
	router: any;
	showStatusScreen: boolean;
  setShowStatusScreen: (val: boolean) => void,
  setStatusData: (val: StatusType) => void,
  successData: StatusType,
	failureData: StatusType,
	userInfo: any;
}

export enum CurrencySymbol {
	INR="₹",
	SGD="$",
	AED = "AED"
}

export enum CurrencyType {
	INR="INR",
	SGD="SGD",
	AED="AED"
}

export interface PriceType {
	symbol: CurrencySymbol,
	currency: CurrencyType,
	basePrice: number,
	lkPrice: number,
}
