import { DataType } from "../../../../Types/general";
import { ConfigDataType } from "../BankOffers/BankOffers.types";

export interface StoreCreditTypes {
  dataLocale: ConfigDataType;
  onSubmit: (...args: any) => Promise<DataType | undefined>;
  removeStoreCredit: (code: string) => Promise<DataType | undefined>;
  appliedCredits?: any[];
  totalAmount:any;
  abandonedLeadsFunction: () => void
}