import React from "react";
import { Dot, TypingLoaderWrapper } from "./styles";

interface TypingLoaderProps {
	show: boolean;
	height?: string;
	width?: string;
  }
const TypingLoader = ({ show,height,width }:TypingLoaderProps) => {
	// const { show,height,width } = props;
	return (
		<TypingLoaderWrapper show={show} >
			<Dot height={height} width={width} />
			<Dot height={height} width={width} />
			<Dot height={height} width={width} />
			<Dot height={height} width={width} />
		</TypingLoaderWrapper>
	);
};

export default TypingLoader;