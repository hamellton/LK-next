import { JSON } from "../../../../Types/general";

export interface GenderRadioPropsType {
  row: number;
  column: number;
  value: string;
  onChange: (gender: string) => void;
  onBlur: (gender: string) => void;
  error?: string;
  touched: boolean;
  localeData: JSON;
  genderConfig: any;
}