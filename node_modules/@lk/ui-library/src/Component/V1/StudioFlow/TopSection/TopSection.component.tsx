import React from "react";
// import SvgBackArrowMsite from "../../../../Icons/BackArrowMsite";
import { ActionContent, BackContainer, Header } from "./TopSection.styles";
import { TopSectionPropTypes } from "./TopSection.types";

function TopSection({ children, goBack, title }: TopSectionPropTypes) {
	return (
		<div>
			<ActionContent>
				<BackContainer onClick={goBack}>
					{/* <SvgBackArrowMsite height={32} width={44} /> */}
				</BackContainer>
				{children}
			</ActionContent>
			<Header>{title}</Header>
		</div>
	);
}

export { TopSection };