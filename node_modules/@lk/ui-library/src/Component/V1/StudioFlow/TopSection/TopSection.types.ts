import React from "react";
import { JSON } from "../../../../Types/general";

export interface TopSectionPropTypes {
  children: React.ReactNode;
  localeData: JSON;
  goBack: () => void;
  title: string;
}