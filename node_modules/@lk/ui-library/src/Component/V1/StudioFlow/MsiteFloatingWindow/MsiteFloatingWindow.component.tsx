import React from "react";
import SvgInfoCircle from "../../../../Icons/InfoCircle";
import { Button, ButtonContainer, FloatingWindowWrapper, Info, InfoContainer, InfoIconContainer } from "./MsiteFloatingWindow.styles";
import { MsiteFloatingWindowPropTypes } from "./MsiteFloatingWindow.types";

function MsiteFloatingWindow({ onClick, localeData, disabled, buttonText }: MsiteFloatingWindowPropTypes) {
	return (
		<FloatingWindowWrapper>
			<InfoContainer>
				<InfoIconContainer>
					<SvgInfoCircle />
				</InfoIconContainer>
				<Info>{ localeData.STUDIOFLOW_INFO as string }</Info>
			</InfoContainer>
			<ButtonContainer>
				<Button
					onClick={onClick}
					tabIndex={0}
					disabled={disabled}
				>
					{buttonText ?? localeData.SELECT_STORE as string}
				</Button>
			</ButtonContainer>
		</FloatingWindowWrapper>
	);
}

export { MsiteFloatingWindow };