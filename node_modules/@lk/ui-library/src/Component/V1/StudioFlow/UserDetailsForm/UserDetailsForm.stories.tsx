import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import { UserDetailsForm } from "./UserDetailsForm.component";
import { FormData } from "./UserDetailsForm.types";
import { action } from "@storybook/addon-actions";

export default {
	title: "Components/v1/StudioFlow/UserDetailsForm",
	component: UserDetailsForm,
	argTypes: {

	}
} as ComponentMeta<typeof UserDetailsForm>;

const Template: ComponentStory<typeof UserDetailsForm> = (args) => <UserDetailsForm {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	submitCallback: (formData: FormData) => action("submitCallback: ")({ formData }),
	initialFormState: {
		firstName: "",
		lastName: "",
		email: "",
		gender: "male",
		phone: "",
		phoneCode: "+65"
	},
	localeData: {
		FIRST_NAME: "First Name",
		LAST_NAME: "Last Name",
		PHONE_NUMBER: "Phone Number",
		EMAIL_ADDRESS: "Email Address"
	},
	isRTL: false
};
