import { JSON } from "../../../../Types/general";

export interface UserDetailsFormPropsType {
  submitCallback: (formData: FormData) => void;
  initialFormState: FormData;
  isRTL: boolean;
  isCLProduct?: boolean;
  tNCLink?: string;
  localeData: JSON;
  genderConfig: any
}

export type FormData = {
  firstName: string;
  lastName: string;
  phone: string;
  phoneCode: string;
  email: string;
  gender: string;
  contactLensChecked?: boolean;
}