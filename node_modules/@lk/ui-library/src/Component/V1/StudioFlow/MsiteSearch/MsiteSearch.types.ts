import { JSON } from "../../../../Types/general";
import { Store } from "../OtherStores/OtherStores.types";

export interface MsiteSearchProps {
  stores: Store[];
  onSelect: (store: Store) => void;
  localeData: JSON;
  storeAvailable:boolean
}