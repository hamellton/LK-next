import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import { NoResults } from "./NoResults.component";

export default {
	title: "Components/v1/StudioFlow/NoResults",
	component: NoResults,
	argTypes: {

	}
} as ComponentMeta<typeof NoResults>;

const Template: ComponentStory<typeof NoResults> = (args) => <NoResults {...args} />;

export const Overview = Template.bind({});

Overview.args = {
	localeData: {
		SORRY_OUT_OF_STOCK_DESKTOP: "Sorry, selected product just went out of stock at all the stores",
		PLEASE_TRY_DIFFERENT_DESKTOP: "Please try a different product, or visit a store near you",
	},
};