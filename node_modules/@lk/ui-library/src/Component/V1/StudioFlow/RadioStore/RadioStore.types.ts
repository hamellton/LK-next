import { JSON } from "../../../../Types/general";
import { Store } from "../OtherStores/OtherStores.types";

export interface RadioStorePropTypes {
  store: Store;
  selected: boolean;
  onClick: (store: Store) => void;
  localeData: JSON;
  resetIsSelectedStore?: () => void;
}
