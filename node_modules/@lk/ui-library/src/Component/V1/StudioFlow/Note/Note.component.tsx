
import React from "react";
import { NoteWrapper } from "./Note.styles";
import { NotePropsType } from "./Note.types";

function Note({ children, font }: NotePropsType) {
	return (
		<NoteWrapper font={font}>{ children }</NoteWrapper>
	);
}

export { Note };