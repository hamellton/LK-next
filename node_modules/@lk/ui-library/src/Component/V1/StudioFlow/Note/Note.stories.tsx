import React from "react";
import { ComponentMeta, ComponentStory } from "@storybook/react";
import { generateArgTypes } from "../../../../StorybookHelper";
import { Note } from "./Note.component";
import { TypographyENUM } from "../../../../Types/general";

export default {
	title: "Components/v1/StudioFlow/Note",
	component: Note,
	argTypes: {
		font: generateArgTypes({
			type: "string",
			required: false,
			description: "Typography of the component",
			category: "General",
			defaultValue: TypographyENUM.lkSansRegular,
			control: "select",
			options: TypographyENUM,
		}),
	},
} as ComponentMeta<typeof Note>;

const Template: ComponentStory<typeof Note> = (args) => (
	<Note {...args} />
);
export const Overview = Template.bind({});

Overview.args = {
	font: TypographyENUM.lkSansRegular,
	children: "Note: Booking an appointment is mandatory for placing an order for progressive lenses"
};
