import styled from "styled-components";
import { TypographyENUM } from "../../../../Types/general";

export const NoteWrapper = styled.div<{font?: TypographyENUM}>`
  font-family: ${({ font }) => font ? `${font}` : TypographyENUM.lkSansRegular};
  font-weight: 400;
  font-size: var(--fs-16);
  line-height: 24px;
  letter-spacing: -0.02em;
  color: var(--dark-blue-100);
  background: var(--tortoiseshell-10);
  border: 1px solid var(--tortoiseshell-50);
  border-radius: var(--border-radius-xxxs);
  padding: var(--pd-16) var(--pd-16) var(--pd-13);
`;