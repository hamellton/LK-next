import styled from "styled-components";
import { TypographyENUM } from "../../../Types/general";

export const SwatchRow = styled.div<{font: TypographyENUM}>`
	font-family: ${({ font }) => font};
	display: flex;
	width: 100%;
	margin-bottom: 18px;
`;
export const SwatchContainer = styled.div<{selected: boolean}>`
	/* border: 1px solid #CECEDF; */
	border-radius: 100px;
	padding: 6px 12px;
	gap: 8px;
	margin-right: 10px;
	height: 30px;
	display: flex;
	cursor: pointer;
	/* opacity: ${props => props.selected ? 0.4 : 1}; */
	${props => props.selected ? `
		background: #F5F5FF;
		border: 1px solid #A2A2B8;
	` : `
		background: #FFFFFF;
		border: 1px solid #CECEDF;
	`}
`;
export const SwatchImg = styled.img`
`;
export const SwatchText = styled.div`
	font-style: normal;
	font-weight: 700;
	font-size: 12px;
	line-height: 18px;
	display: flex;
	font-family: ${TypographyENUM.lkSansBold};
	align-items: center;
	letter-spacing: -0.02em;
	color: var(--dark-blue-100);
`;
