import { DataType, DeviceTypes } from "../../../../Types/general";

export interface ContactLensConsentCheckboxType {
    checked: boolean
    toggleChecked: () => void
    dataLocale: DataType;
    deviceType?: DeviceTypes
    configData: DataType;
}