export interface NewRadioGroupType {
	data: {key:string, value:string}[];
	row: number;
	column: number;
	isRequired: boolean;
}

export interface RadioType {
	isSelected: boolean;
	onClick: () => void;
}
