export interface SliderCarouselType {
	id: string,
	sliderImages: SliderImageType[],
	duration: number | string,
	autoPlay: boolean
	isRTL?: boolean
}

export interface SliderImageType {
	id: string | number,
	imageUrl: string
}
