import { DataType, TypographyENUM } from "../../../../Types/general";
import { AddressType, SubmitType } from "../AddAddressForm/AddAddressForm.types";

export interface AddressTabTypes {
	id: any
	width: string
	addressline1: string
	addressline2: string
	city: string
	country: string
	firstName: string
	landmark: string
	lastName: string
	phone: string
	postcode: string
	state: string
	selectedAddressIndex: {id: string | number}
	checked: boolean
	onChange: (id: number) => void
	onEdit: (id: number) => void
	onAdd: () => void,
	onDelete: (id: number) => void,
	addressLabel: string,
	font: TypographyENUM
	deliveryEstimate: string
	email: string;
	onSubmit: (type: SubmitType, id: number | null, props: AddressType) => void;
	submitRef: React.Ref<HTMLButtonElement>
	addAddressBtn: boolean;
	phoneCode: string,
	gender: string | null,
	dataLocale: DataType,
}
