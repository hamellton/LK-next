import React from "react";
// import AddressInputField from "../../components/AddressInputField";
// import AddressTextField from "../../components/AddressTextField";

const NormalField = () => { //data: any
	return (
		<div></div>
		// <AddressInputField subText={data.subText} errorMessage={data.props.errorMessage} isError={false} focused={focusedId === data.props.id}>
		// 	<AddressTextField value={values[data.props.name]} onChange={onChange} clearText={() => clearValue(data.props.name, data.defaultValue)}
		// 		onBlur={() => handleFocus(data.props.id)}
		// 		{...data.props}
		// 		// onBlur={(e) => errorHandler("firstName", validateRequired, e.target.value)}
		// 	></AddressTextField>
		// </AddressInputField>
	);
};

export default NormalField;
