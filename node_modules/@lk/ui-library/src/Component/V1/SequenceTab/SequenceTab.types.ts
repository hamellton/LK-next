export interface SequenceTabType {
	topText?: string,
	btText: string,
	currentlySelected: boolean,
	enabled: boolean,
	id: string,
	onClick: (prop: any) => void,
	showDividerLine: boolean
	isRTL: boolean
}
