import styled from "styled-components";
import { TypographyENUM } from "../../../Types/general";

export const AuthorizationContainer = styled.div<{styleFont:TypographyENUM}>`
display:flex;
background-color:var(--white);
width:100%;
font-family:${props => props.styleFont};
border-radius: 6px;
overflow: hidden;
`;
export const BannerWrapper = styled.div`
`;

export const Banner = styled.img`
height:100%`;

export const AuthWrapper = styled.div`
display:flex;
flex-direction:column;
width:100%;
padding:0 var(--pd-15);
`;

export const AuthHeader = styled.ul`
list-style: none;
padding:var(--pd-10);
margin:0;
display:flex;
gap:var(--spacing-md);
font-size: 13px;
`;
export const AuthHeaderTab = styled.li<{selected:boolean}>`
font-size: 13px;
padding:var(--pd-5) 0;
cursor: var(--pointer);
color: ${props => props.selected? "var(--turquoise)" : "var(--gray)"};
border-bottom: 1px solid ${props => props.selected? "var(--turquoise)" : "var(--transparent)"};
`;
