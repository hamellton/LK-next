import styled from "styled-components";

export const ImageWrapper = styled.a`
    width:100%;
    cursor: pointer;
`;

export const Image = styled.img`
    width:100%
`;

export const ImageContainer = styled.a`
    padding:0px 4px 0px 4px;
    width: 100%;
    height: 100%;
    display: block;
	&:focus{
		border:none;
		outline:none;
	}
`;

export const ButtonContainer = styled.div`

`;

export const Arrow = styled.div<{right?: boolean}>`
    position: absolute;
    z-index: 1;
    top: 43.5%;
    ${props => props?.right ? "right: 48px" : "left: 48px"};

    span{
        font-weight: 700;
        font-size: 45px;
        color: white;
        cursor: pointer;
    }
`;

export const Relative = styled.div`
position: relative;
`;


export const MobileArrow = styled.div<{right?: boolean}>`
    position: absolute;
    z-index: 1;
    top: 43.5%;
    ${props => props?.right ? "right: 0" : "left: 0"};
    font-size: 45px;
    color: #9e9e9e;
    font-weight: 700;
`;
