import React from "react";
import Carousel from "../../../Common/Carousel";
import { ProductImageCarouselType } from "./WearTrendWithCarousel.types";
import { Image, WearTrendWithCarouselWrapper, ImageWrapper, ImageText } from "./styles";
import PrimaryButton from "../../../Common/PrimaryButton";
import { ThemeENUM } from "../../../../Types/general";

const WearTrendWithCarousel = ({ id, images, font }: ProductImageCarouselType) =>{
	const carouselRef = React.useRef(null);

	return <WearTrendWithCarouselWrapper id={id} font={font}>
		<Carousel slidesToShow={4} wrapperClassName="image-slider" carouselConfig={{ speed:0, infinite:true, slidesToScroll:4 }} ref={carouselRef}>
			{images.map((image, index) => (
				<Carousel.Slide id={index} currentSlide={1} key={index}>
					<ImageWrapper>
						<Image src={image.source} />
						<ImageText>{image.text}</ImageText>
						<PrimaryButton font={font} id="btn-explore" height="45px" width="140px" onBtnClick={()=>console.log("redirect")} theme={ThemeENUM.primary} primaryText="Explore"/>
					</ImageWrapper>
				</Carousel.Slide>
			))}
		</Carousel>

		{/* <WearTrendTextWrapper>
			<WearTrendText>WEAR</WearTrendText>
			<WearTrendText>THE</WearTrendText>
			<WearTrendText>TREND</WearTrendText>
			<WearTrendSubText>
				Our hottest collections
			</WearTrendSubText>
		</WearTrendTextWrapper> */}
		{/* <CarouselWrapper> */}
	</WearTrendWithCarouselWrapper>;

};

export default WearTrendWithCarousel;
